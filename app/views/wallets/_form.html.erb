<%= simple_form_for @wallet do |form| %>
  <% if @wallet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wallet.errors.count, "error") %> prohibited this wallet from being saved:</h2>

      <ul>
        <% @wallet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">

  <div class="col-4">
  </div>

  <div class="col-4">
    <div class="from-group">
      <%= form.label :walletbalance %>
      <%= form.number_field :walletbalance ,required: true, min: 1,class:"form-control"%>
    </div>

    <div class="actions">
       <%= form.button :submit, class: 'btn btn-success' %>
    </div>

    <% end %>
  </div>

  <div class="col-4">
  </div>
  
</div>
  
